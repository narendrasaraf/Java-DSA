public class find_peak_element {
    public static int optimal(int[] nums) {
        int n=nums.length;
        int low=0;
        int high=n-1;
        while(low<=high) {
            int mid=(low+high)/2;

        }
        
        return -1;
    }
}
